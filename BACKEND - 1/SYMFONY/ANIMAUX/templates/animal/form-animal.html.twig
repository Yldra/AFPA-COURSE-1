{% extends 'base.html.twig' %}

{% block title %}FORMULAIRE ANIMAL{% endblock %}

{% block body %}

<div class="container text-center mt-5" style="width: 25%;">

    {{ form_start(aform) }}

    <div class="form-group">
        {{ form_row(aform.nom, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Nom: '
        }) }}
    </div>

    <div class="form-group">
        {{ form_row(aform.description, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Description: '
        }) }}
    </div>

    <div class="form-group">
        {{ form_row(aform.image, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Image: '
        }) }}
    </div>

    <div class="form-group">
        {{ form_row(aform.nom_latin, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Nom Latin: '
        }) }}
    </div>

    <div class="form-group">
        {{ form_row(aform.espece, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Espèce: '
        }) }}
    </div>

    <div class="form-group">
        {{ form_row(aform.continents, {
            'attr': {'class': 'form-control form-control-sm'},
            'label': 'Continent: '
        }) }}
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>

    {{ form_end(aform) }}

</div>

{% endblock %}
